import endpoint from './endpoint.js';

const {
  actionCreators,
  middleware,
  reducer,
  reducerObj,
  selector,
} = endpoint;

export {
  actionCreators,
  middleware,
  reducerObj,
  selector,
};

export default reducer;
